<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:GUI.Converters"
             x:Class="GUI.Views.ItemDetailPage"
             Title="{Binding Title}">

    <ContentPage.Resources>
        <converters:PaidColorConverter x:Key="PaidColorConverter" />
    </ContentPage.Resources>

    <StackLayout Spacing="20"
                 Padding="15"
                 BackgroundColor="{Binding Item.ItemHasPaid, Converter={StaticResource PaidColorConverter}}">
        <Label Text="Owner" 
               FontAttributes="Bold"
               FontSize="20" />
        <Label Text="{Binding Item.Owner}"
               FontSize="25"/>

        <Label Text="Boat"
               FontAttributes="Bold"
               FontSize="20" />
        <Label Text="{Binding Item.BoatClass}"
               FontSize="25"/>

        <Label Text="Zone"
               FontAttributes="Bold"
               FontSize="20" />
        <Label Text="{Binding Item.Zone}"
               FontSize="25" />

        <Label Text="DueDate"
               FontAttributes="Bold"
               FontSize="20" />
        <Label Text="{Binding Item.DueDate}"
               FontSize="25"/>
    </StackLayout>
    
</ContentPage>